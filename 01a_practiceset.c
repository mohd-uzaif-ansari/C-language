// using hardcode input ~ mohd-uzaif-ansari
#include <stdio.h>

int main()
{
    //calculating area of rectangle
    int length = 2;
    int breadth = 3;
    int area = length * breadth;
    printf("Area of rectangle is : %d", area);
    return 0;
}